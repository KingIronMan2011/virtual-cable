{
  "name": "virtual-cable-analytics",
  "version": "1.4.6",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute virtual-cable-analytics --file=./schema.sql --remote",
    "db:init:local": "wrangler d1 execute virtual-cable-analytics --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260904.1",
    "wrangler": "^4.129.0"
  }
}
